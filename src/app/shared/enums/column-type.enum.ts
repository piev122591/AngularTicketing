export enum ColumnType {
  STRING = 'string',
  INT = 'int',
  LONG = 'long',
  BOOL = 'bool',
  DATE = 'date',
  DATETIME = 'datetime',
  DOUBLE = 'double',
  BINARY = 'binary',
  DICTIONARY = 'dictionary', // select box
  ENUM = 'enum', // select box
}
